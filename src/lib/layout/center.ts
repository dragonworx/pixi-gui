import Layout, { Cell, LayoutAlign, Orientation } from 'src/lib/layout/layout';
import Rectangle from 'src/lib/rectangle';
import { Position } from '../display/style';
import Box from '../node/box';

export default class CenterLayout extends Layout {
  apply(hAlign: LayoutAlign, vAlign: LayoutAlign): void {
    // const { root } = this;
    // const children = [...root.children] as Box[];
  }
}
